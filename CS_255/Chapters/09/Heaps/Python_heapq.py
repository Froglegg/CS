import heapq

'''Heapq module provides functions that allow a standard Python list to be managed as a heap.
elements serve as their own key, however
'''
example = [12, 3, 5, 23, 5, 11, 16, 12]
heapq.heapify(example)
print(example)
